<ul id="read_receipts_list">
    {{#each users}}
        <li>
            <img class="read_receipts_user_avatar" src="{{avatar_url}}" />
            <span>{{full_name}}</span>
        </li>
    {{/each}}
</ul>
